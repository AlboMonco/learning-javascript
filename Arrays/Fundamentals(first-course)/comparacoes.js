// == (comparação implícita)

const numero = 4;
const texto = "4";

console.log(numero === texto);

//typeof
console.log(typeof numero);
console.log(typeof texto);

// == só compara o valor
// === compara o valor e o tipo de dado
// Boa prática diz para usar somente o ===
